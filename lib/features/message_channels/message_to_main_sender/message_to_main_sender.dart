import 'package:flutter/material.dart';

class MessageToMainSender extends StatelessWidget {
  const MessageToMainSender({
    super.key,
    required this.child,
  });

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}

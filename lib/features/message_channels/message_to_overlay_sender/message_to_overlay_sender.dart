import 'package:flutter/material.dart';

class MessageToOverlaySender extends StatelessWidget {
  const MessageToOverlaySender({
    super.key,
    required this.child,
  });

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}

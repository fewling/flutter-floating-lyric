import 'package:flutter/material.dart';

class OverlayWindowSettings extends StatelessWidget {
  const OverlayWindowSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
